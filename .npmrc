# NPM Configuration for expo-bixolon
# Registry configuration
registry=https://registry.npmjs.org/

# Access level - public for open source packages
# access=public

# Optional: Configure scope if publishing to an organization
# @sincpro-srl:registry=https://registry.npmjs.org/
